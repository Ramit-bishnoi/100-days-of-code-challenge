vector<int> Solution::solve(vector<int> &A) {
    int i = 0;
    int j = 0;
    for(i = 0 ; i < A.size() ; i++)
    {
        if(A[i] == 0)
        {
            swap(A[j] , A[i]);
            j++;
        }
    }
    return A;
}

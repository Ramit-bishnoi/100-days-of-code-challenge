int Solution::solve(int A) {
    A = abs(A);
    
    int count = 0, sum = 0;
    while(sum < A || (sum - A) % 2 !=0 )
    {
        count++;
        sum = sum + count ;
    }
    return count;
    
}
